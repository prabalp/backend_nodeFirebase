const db = firebase.database();

export const add_db = async () => {
  // add new entry to the db
};

export const read_db = async () => {
  //read from db
};

export const update_db = async () => {
  //update from db
};

export const delete_db = async () => {
  //delete from db
};
